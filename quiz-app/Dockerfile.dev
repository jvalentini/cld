# Dockerfile for running the quiz app in development mode
# Used by e2e tests to have a target to test against

FROM node:18-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm ci

# Copy source code
COPY . .

# Expose dev server port
EXPOSE 3000

# Start the development server with host binding
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]

